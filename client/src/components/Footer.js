import { StyledFooter } from "../styled/Footer.styled";

const Footer = () => {
    return (
        <StyledFooter>
            <h1 className="logo">Restaurant</h1>
        </StyledFooter>
      );
}
 
export default Footer;
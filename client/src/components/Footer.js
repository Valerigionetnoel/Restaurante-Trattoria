import { StyledFooter } from "../styled/Footer.styled";

const Footer = () => {
    return (
        <StyledFooter>
            <h2 className="logo">Ristorante Trattoria</h2>
        </StyledFooter>
      );
}
 
export default Footer;